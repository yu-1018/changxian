# 上架文案 · STORE LISTING

用于 GitHub 仓库简介、WorkBuddy 技能市场介绍的对外话术。可直接复制粘贴。

## 一句话简介

> 把一个长期目标，变成一台无人值守、可断点续跑、抗限流的长循环任务机器。

## 短简介（≤50 字，用于市场列表）

> 长任务自动跑到底：状态落盘不丢、上下文永不爆炸、限流自动退避。

## 长简介（用于详情页）

SOLO-LOOP 让 AI 从「问一句答一句」升级为「给个目标，自己跑到底」。

它把长期目标拆成一棵可执行的任务树，按定时唤醒逐轮推进：**调度器只做对账和派发**，真正的活交给隔离运行的子任务，子任务只回 120 字以内的结论、产物写进磁盘。所以无论跑 1 轮还是 200 轮，上下文永远只有「这一轮」那么大。

崩溃、断网、重启都不会丢进度——因为所有状态都在 `state.json` 里，下一轮读盘接着跑。任务完成、预算耗尽、或连续多轮没有进展时，它会自动写终报告并注销自己，不会留在后台空转烧钱。

## 核心优势（5 条）

1. **上下文永不爆炸**：调度器不读产物正文，只判文件在不在；子任务隔离执行、只回一句话。跑 200 轮和第 1 轮一样轻。
2. **断电续跑，零丢失**：任务树、进度、预算、限流状态全部落盘，进程崩溃/机器重启后读盘即续。
3. **不会空转烧钱**：三条停机线（任务完成 / 预算耗尽 / 连续零进展），命中即写报告并删除自己的定时任务。
4. **抗限流设计**：并发恒为 1 + 定时抖动 + 冷却门 + 多模型降级；限流被当作「稍后再试」而不是失败，更不会误触停机。
5. **零依赖零门槛**：三个纯标准库 Python 脚本，无需服务器、无需装包、无需付费服务；有定时器就自动化，没有就手动跑一轮，协议一致。

## 适用人群与场景

- 需要**跨天/跨周推进**的大任务：资料整理、内容批量生产、代码库迁移、课程/知识库构建。
- 想让 AI **自己拆任务、自己派活**，而不是每步都要人盯着。
- 经常遭遇 **API 限流**、长会话上下文爆炸、或任务做到一半断掉的人。

## 使用示例

```
python scripts/solo_init.py --root ./my-loop \
  --goal "把日语 N2 语法整理成 Anki 卡组：覆盖全部条目，输出 artifacts/n2/cards.csv，≥120 条，抽检 10 条无重复无空字段"
```

随后按 `cron-payload.json` 建一个每 10 分钟的定时任务，就再也不用管它了。

## 技术亮点（给开发者看）

- 惰性展开：只在分支轮到执行时才拆下一层，拒绝过度规划与返工。
- 叶子硬判据：单次会话 ≤15 分钟、≤1 个交付物、≤3 步、零人工输入。
- 回执协议：`DONE / BLOCKED / TOO_BIG` 三态，任务太大自动触发再拆。
- 结构：`SKILL.md` + `scripts/` + `templates/` + `references/`，与主流 AgentSkill 规范一致。

## 常见问题

- **必须有定时器吗？** 不必，手动「跑一轮」即可，状态机制完全一样。
- **会烧很多钱吗？** 相反：并发 1、每轮新会话、预算上限 + 停机线，是省钱的写法。
- **支持哪些模型？** 不限厂商；`state.json.models` 里按顺序填降级链即可。
- **数据安全？** 全部状态都在你的本地工作目录，不上传任何第三方。
